/*
 *  Stub header for rtc_base/thread.h
 *  The full threading infrastructure is not available in this project.
 *  This stub allows tests to compile that include thread.h but don't
 *  actually use threading features.
 */

#ifndef TESTS_TEST_UTILS_THREAD_STUB_H_
#define TESTS_TEST_UTILS_THREAD_STUB_H_

// Empty stub - tests that actually need threading will fail at runtime,
// but tests that just include thread.h without using it will work.

#endif  // TESTS_TEST_UTILS_THREAD_STUB_H_
